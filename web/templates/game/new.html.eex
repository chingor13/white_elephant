<h2>New game</h2>
<%= render "form.html", changeset: @changeset,
                        action: game_path(@conn, :create),
                        timestamp: @timestamp %>

<%= link "Back", to: game_path(@conn, :index) %>
