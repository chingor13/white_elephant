<h2>Admin for <%= @game.name %> <small>(<%= @game.date %>)</small></h2>

<div id="game" data-game="<%=@game.code%>">
  <form id="new_item" action="#" class="form-inline">
    <div class="form-group">
      <input class="form-control" type="text" name="item[name]" placeholder="New Item" />
      <a href="#" class="btn btn-success">Add Item</a>
    </div>
  </form>

  <table class="table">
    <thead>
      <tr>
        <th>Item</th>
        <th>Steals</th>
        <th></th>
      </tr>
    </thead>
    <tbody>
      <%= for item <- @game.items do %>
      <tr>
        <td><%= item.name %></td>
        <td>
          <a href="#" class="btn btn-default">-</a>
          <%= item.steals %>
          <a href="#" class="btn btn-default">+</a>
        </td>
        <td>
          <div class='pull-right'>
            <%= link "Delete", to: game_item_path(@conn, :delete, @conn.assigns[:game], item), method: :delete, data: [confirm: "Are you sure?"], class: "btn btn-danger" %>
          </div>
        </td>
      </tr>
      <% end %>
    </tbody>
  </table>
</div>
<script src="<%= static_path(@conn, "/js/game.js") %>"></script>