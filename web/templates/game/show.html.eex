<h2><%= @game.name %></h2>

<table class="table">
  <thead>
    <tr>
      <th>Item</th>
      <th>Steals</th>
    </tr>
  </thead>
  <tbody>
    <%= for item <- @game.items do %>
    <tr>
      <td><%= item.name %></td>
      <td><%= item.steals %></td>
    </tr>
    <% end %>
  </tbody>
</table>

<%= link "New item", to: game_item_path(@conn, :new, @conn.assigns[:game]) %>

<%= link "Edit", to: game_path(@conn, :edit, @game) %>
<%= link "Back", to: game_path(@conn, :index) %>
